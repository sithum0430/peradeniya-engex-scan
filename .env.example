# PostgreSQL connection string format OR separate parts
DATABASE_URL=postgres://postgres:postgres@localhost:5432/crowd_db

# Port for the backend
PORT=4000

# Optional: if behind proxy / reverse etc
TRUST_PROXY=false
